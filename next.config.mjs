/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Lomba-webdev',
  assetPrefix: '/Lomba-webdev',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
